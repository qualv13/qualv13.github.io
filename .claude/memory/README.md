# AI session memory

Snapshot of project memory for AI-assisted development sessions, so context
survives across machines and sessions. Read these at session start; update
them when project decisions change (and mirror updates to your local profile
memory if you maintain one).

This repo is public — keep these files free of secrets, tokens and private
contact details.
